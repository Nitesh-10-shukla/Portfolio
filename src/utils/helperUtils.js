export const goToApplication = (url) => {
    window.open(url, '_blank');
  };

const info = ["I am a Software Developer"];
export const autoTypingConf={
  strings: info,
  autoStart: true,
  loop: true,
}